# BookmarkCleaner
This is a small project that solves a very specific problem, It basically checks all bookmark of a browser And then tries to locate are they still working or not if they're working then it leaves them as it is if they are not working then it resolves them ( Basically either it removes them or it replaces them with their redirected links ) 
